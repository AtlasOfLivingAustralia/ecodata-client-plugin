rootProject.name='ecodata-client-plugin'

if (settings.hasProperty('inplace') && Boolean.valueOf(settings.inplace)) {
    include ':ala-map-plugin'
    project(':ala-map-plugin').projectDir = new File(settingsDir,'../ala-map-plugin')
}